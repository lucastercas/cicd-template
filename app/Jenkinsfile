pipeline {
    agent any
    environment {
        PROJECT_KEY = 'org.springframework:gs-spring-boot-docker'
        SONAR_LOGIN = 'e6f696687d9b343872358aaf19371fe5dfdca63f'
        SONAR_URL = 'http://sonarqube:9000'
    }
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build Package') {
            steps {
                sh "make build"
            }
        }
        stage('Test') {
            steps {
                sh 'make test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        stage('Sonarqube') {
            steps {
                sh "mvn sonar:sonar -Dsonar.projectKey=${PROJECT_KEY} -Dsonar.host.url=${SONAR_URL} -Dsonar.login=${SONAR_LOGIN}"
            }
        }
        stage('Docker Login') {
            steps {
                sh "docker login -u ${DOCKER_USER} -p ${DOCKER_PASSWORD}"
            }
        }
        stage('Build Docker Image') {
            steps {
                sh 'make build_image'
            }
        }
        stage('Push Image') {
            steps {
                sh 'make push_image'
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: "target/*.jar", fingerprint: true
        }
    }
}